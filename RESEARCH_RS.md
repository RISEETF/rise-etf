# 연구용 통합 가격 RS v1

## 목적과 범위

국내 ETF·해외 ETF·해외주식을 동일한 시범 풀에서 실제 계산한다. 공식 지수의 환율 규칙 재현과 최종 대표자산 선정은 독립 과제다. 사용자가 승인한 ECB 기준환율을 연구용 환산에 사용하며 종가 환율로 부르지 않는다. 연구용 결과는 `data/research/rs.json`; 기존 `data/universe/status.json`과 검증된 투자 신호의 보류 상태는 유지한다.

## 계산 정의

- 현재 수집 설정의 6개 종목 전부를 포함한다. SPY는 비교용 기준이며 시장 전체의 유일한 대표로 확정한 것이 아니다.
- 수집 시각이 명시된 cutoff 이하인 최신 성공 스냅샷을 종목별로 선택한다. 최초 공표시각 기반 백테스트를 의미하지 않는다.
- 국내외 모두 `close` 필드 사용. 해외 adjusted close와 국내 미확인 조정가격을 총수익률로 혼합하지 않는다. 국내 제공처 조정 규칙 자체는 미확인이므로 엄밀히 검증된 가격수익률도 아니다.
- 모든 요청 종목 가격과 USD/KRW 기준환율에 공통으로 존재하는 최신 관측일을 종료일로 사용한다. 한 종목이 누락되면 자동으로 제외하지 않고 계산을 보류한다.
- 시작 목표일은 종료일에서 7·30·60 **달력일** 전이다. 그 날 또는 이전 7일 안의 마지막 공통 관측일을 실제 시작일로 사용한다. 실제 날짜·일수를 표시한다. 관측값을 채우거나 날짜를 이동하지 않는다.
- 시작·종료일 가격 및 환율의 날짜 차이는 0일이다. 이는 동시 마감이나 동일 정보 시점을 뜻하지 않는다. 거래일 공백이 있는 기간도 양 끝 관측값으로 계산하며 연속 일별 데이터 완전성을 주장하지 않는다.
- 현지 수익률 = 종료 종가 / 시작 종가 − 1.
- USD 자산의 원화 수익률 = (종료 종가 / 시작 종가) × (종료 KRW per USD / 시작 KRW per USD) − 1.
- KRW 자산은 별도 환산하지 않는다. 해당 상품에 경제적 환노출이 없다는 의미가 아니다.
- RS = (1 + 해당 수익률) / (1 + 같은 통화 기준 SPY 수익률) − 1. 0이면 동일 성과다. 원화 RS를 화면에, 현지통화 RS도 JSON에 제공한다.
- 순위는 풀 안의 수익률 내림차순. 동률(퍼센트 값 소수점 10자리 기준)은 공동 순위, 다음 순위는 건너뛴다. 화면은 소수점 2자리라 표시상 같은 값이 다른 순위일 수 있다.

## 해석 제한

분배금 재투자·배당·세금·거래비용·헤지 비용을 추가 계산하지 않는다. 확인된 주식분할이 구간 안에 있으면 raw close 왜곡을 막기 위해 그 기간 전체 비교를 보류한다. 국내 미수집 기업행사 및 제공처 오류까지 해결한 것은 아니다.

현지통화 순위는 각 자산의 표시통화 가격 변화 비교다. 원화 투자자의 성과 비교에는 원화 열을 사용한다. KR 상장 해외 ETF 종가는 이미 환율·괴리율·거래시간 영향이 포함돼 있으므로 SPY 원화환산과 일치할 필요가 없다. 위험조정, 듀레이션 보정, 매매 임계치 또는 매수 추천을 뜻하지 않는다.

## 실행과 갱신

```sh
python scripts/build_research_db.py
python scripts/build_research_rs.py --cutoff 2026-09-18T08:00:00+09:00
```

cutoff 미지정 시 실행 시각 UTC를 사용한다. 시계열 수집 워크플로의 DB 재구축 다음에 자동 실행한다. 관측 종료일·수집 원본 해시/URL/시각·실제 입력 가격과 환율을 결과에 보존한다. 화면은 계산 시각과 종료일의 경과 일수를 표시하며 오래된 데이터를 오늘 시세로 표시하지 않는다. 빌드 실패 시 이전 결과가 남을 수 있으므로 갱신 시각을 확인한다.

## 검증

### 화면 비교와 최신성 안내

원화/현지통화 선택은 순위 정렬과 SPY 대비 RS 열을 동시에 바꾸며 두 통화의 수익률은 계속 나란히 표시한다. 환산에 따른 차이(%p)는 원화 수익률에서 현지통화 수익률을 뺀 값으로, 가격과 환율의 교차효과가 포함된다. 순위 차이는 통화 기준 변경에 따른 차이이며 전일 대비 순위 변화가 아니다. 양수 종목 수는 6개 시범 풀의 요약으로 시장 전체 상승 참여도로 해석하지 않는다.

공통 관측일이 한국 날짜 기준 7달력일 이상 이전이면 과거 비교값 안내를 표시한다. 이 임계치는 화면 최신성 안내일 뿐 휴장일이나 수집 장애를 판정하지 않는다. 종목별 최근 가격 관측일·수집 시각, ECB 수집 시각을 펼쳐서 확인할 수 있다. 분모가 되는 시범 풀과 계산식은 바뀌지 않는다.

합성 예제: USD 가격 +5%, KRW/USD +10%이면 원화 +15.5%. KRW 가격 +10%는 현지순위 1위, 원화순위 2위가 된다. 테스트는 이 순위 역전, SPY 대비 비율, 동률, 결측 환율/종목, 잘못된 값·중복·미래 수집시각, 주식분할에 대한 보류를 검증한다. 브라우저 테스트는 기간 전환·행 수·날짜 표시와 자료 누락 시 빈 화면 안내를 확인한다.
