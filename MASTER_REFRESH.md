# 종목 목록 자동 갱신

## 2026-09-22 수정 사유

기존 `riseetf.co.kr/prod/finder` 주소는 개편된 `kbam.co.kr` 홈페이지로 리다이렉트됐다. 구형 HTML 목록 수집기가 실패해 최근 성공본 143개가 남아 있었다. 화면 숫자는 고정값이 아니라 `data/master/latest.json`의 종목 수다.

공식 새 찾기 화면은 https://kbam.co.kr/find 이며 해당 화면의 공개 프런트엔드가 사용하는 목록 경로는 https://kbam.co.kr/api/products/etfs 이다. 페이지 수·총수량을 읽어 모든 페이지를 수집하고, 공식 화면 수량과 API 총수량, 페이지 순서·총 페이지 수·중복 코드·고유 상품 ID를 대조한다. 숫자 144를 목표 수량으로 하드코딩하지 않는다. 기록된 과거 143개 스냅샷은 과거 사실이므로 수정하지 않는다.

## 갱신 및 표시

- 평일 한국시간 07:17 사전 확인, 09:17~18:47 매시 17분·47분에 확인한다.
- GitHub Actions 수동 실행도 가능하다: `RISE official instrument master` → Run workflow.
- 성공하면 최신 종목 수·목록·원문 해시·수집 시각을 저장하고 자동 배포를 요청한다.
- 공식 사이트 게시, GitHub 예약 실행, Pages 배포 지연이 있으므로 실시간이나 30분 내 완료를 보장하지 않는다.
- 실패하거나 페이지가 불완전하면 최신 목록을 교체하지 않고 실패 이력을 표시한다.
- 새 API에서 제공하지 않는 연금 적격 라벨·보수는 빈 값으로 남긴다. 예전 값을 현재 사실로 복사하지 않는다.

## 변동 내역

`source_product_count`는 공식 전체 상품 수, `instrument_count`는 거래소 코드가 확인된 상품 수다. 공식 API의 코드가 비어 있는 상품은 `pending_products`에 보관하고 화면에는 코드 확인 대기로 포함한다. 2026-09-22 수집본은 전체 144개, 코드 확인 143개, 대기 1개다. 공식 상품 ID로 연결하므로 추후 코드가 제공되면 `CODE_UPDATED`로 기록하고 중복 편입하지 않는다. 코드 대기 상품을 가격·RS 분석에 임의 연결하지 않는다.

`data/master/changes.json`은 이전 성공 목록 대비 종목 추가·제외·명칭 변경을 보관한다. 기준일과 최초 발견 시각, 전후 원문 해시를 함께 저장한다. 첫 기준 목록은 신규 상장 이벤트로 만들지 않는다. 목록이 같으면 이벤트를 중복 생성하지 않는다.

목록 추가는 공식 목록에 새로 관측됐다는 뜻이다. 신규 상장 여부는 함께 표시한 공식 상장일과 구분한다. 목록 제외는 공식 페이지에서 빠졌다는 관측이며 법적 상장폐지 확정이 아니다. 거래소 공시 대조 전에는 상장폐지일을 만들지 않는다. 제외돼도 과거 스냅샷과 가격 데이터는 삭제하지 않는다.

공식 목록에 추가된 상품이 자동으로 가격 수집 시범 풀이나 RS 대표자산에 편입되는 것은 아니다. 거래소 기반 상장·폐지 상태와 운용사 상품 상세정보를 분리하는 후속 통합이 필요하다.
